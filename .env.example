# sol_beast Environment Variables
# Copy this file to .env and fill in your values
# These override settings in config.toml

# Most secure way to store your trading wallet keypair
# Create with: export SOL_BEAST_KEYPAIR_B64=$(cat keypair.json | base64 -w0)
# SOL_BEAST_KEYPAIR_B64=YOUR_BASE64_ENCODED_KEYPAIR_HERE

# Simulation wallet (for testing without risking real funds)
# SOL_BEAST_SIMULATE_KEYPAIR_B64=YOUR_TEST_WALLET_BASE64_HERE

# Optional: Override config file path
# SOL_BEAST_CONFIG_PATH=/path/to/config.toml

# Rust logging level
RUST_LOG=info

# Note: License key and dev fee wallet MUST be in config.toml
# They cannot be set via environment variables for security reasons
